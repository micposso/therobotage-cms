{
  "name": "therobotage",
  "private": true,
  "scripts": {
    "dev": "concurrently -n cms,web -c magenta,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:cms": "npm run develop --prefix backend",
    "dev:web": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
